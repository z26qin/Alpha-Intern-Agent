"""Tools an LLM agent can later call.

This package is intentionally empty for now. In a future PR, each
research capability (load_data, build_features, run_backtest,
add_memory, etc.) will be exposed here with a JSON schema so an LLM
agent can orchestrate them.
"""
